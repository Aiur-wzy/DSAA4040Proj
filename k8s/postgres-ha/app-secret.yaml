apiVersion: v1
kind: Secret
metadata:
  name: bookstore-postgres-app
  namespace: bookstore
type: kubernetes.io/basic-auth
# Demo-only credentials. Keep synchronized with k8s/secret.yaml unless you
# also update bookstore-secret DB_USER/DB_PASSWORD for the backends/init job.
stringData:
  username: bookstore
  password: bookstore123
